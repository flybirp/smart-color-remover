---
name: smart-color-remover
description: 智能颜色抠图器 - 将图片中指定颜色变成透明（生成带 Alpha 通道的 PNG）。使用场景：用户要求"扣掉/去掉/移除图片中的某种颜色"、"把背景色变透明"、"颜色抠图"、"去色变透明"、"制作透明背景图片"。关键词：抠图、抠色、去色、透明、alpha、颜色移除、背景移除。支持中文颜色名（红/浅蓝/深绿/白色背景…）、十六进制、RGB 输入。
---

# Smart Color Remover（智能颜色抠图器）

输入一张图片 + 颜色描述文字（如"扣掉红色"），智能识别并匹配该颜色，将其变成透明，输出带 Alpha 通道的 PNG。

## 何时使用

用户提出类似需求时触发：
- "把这张图的红色扣掉/变透明"
- "去掉图片的白色背景"
- "将 logo 的蓝色背景变透明"
- "扣掉浅粉色"
- "这个图把 #00FF00 的地方抠成透明"

## 快速开始

**第一步**：从用户话术中提取颜色关键词（脚本会自动清洗"扣掉/背景/透明"等干扰词，直接传原话也可以，但提取后更可靠）。

**第二步**：运行脚本：

```bash
python3 scripts/color_remover.py <输入图片> --color "<颜色>" [选项]
```

**第三步**：把输出的 `*_transparent.png` 展示给用户。

## 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--color, -c` | 要抠掉的颜色（必填），见下方"颜色输入格式" | - |
| `--output, -o` | 输出 PNG 路径 | `<原名>_transparent.png` |
| `--tolerance, -t` | 容差 0-100，越大抠得越宽 | 25 |
| `--feather, -f` | 边缘羽化半径 0-5，平滑边缘抗锯齿 | 0 |
| `--invert` | 反选：保留该颜色，其余变透明 | 关 |
| `--background-only` | 只抠与图片边缘连通的区域（保护物体内部同色区域） | 关 |
| `--min-region` | 移除小于 N 像素的孤立噪点区域 | 0 |
| `--json` | 以 JSON 输出统计结果 | 关 |

## 颜色输入格式

| 格式 | 示例 | 匹配方式 |
|------|------|----------|
| 中文颜色名 | `红`、`橙`、`黄`、`绿`、`青`、`蓝`、`紫`、`粉`、`品红`、`棕`、`黑`、`白`、`灰`、`金`、`银`、`米` | HSV 色相范围匹配，同色系深浅变体（如深红、浅红）都能匹配 |
| 深浅修饰 | `浅红`、`淡蓝`、`深绿`、`暗棕`、`亮黄`、`鲜艳橙` | 修饰词自动调整明度/饱和度匹配窗口 |
| 英文颜色名 | `red`、`light blue`、`dark green`、`pink` | 同中文 |
| 十六进制 | `#FF0000`、`#F00` | 感知加权 RGB 距离匹配（精确值，只抠相近色） |
| RGB | `rgb(255,0,0)`、`255,0,0` | 同十六进制 |

**颜色名 vs 精确值的区别**：`--color "红"` 走 HSV 色相匹配，能覆盖大红、深红、暗红等整个红色系；`--color "#FF0000"` 只抠与该精确值相近的颜色。用户想要"扣掉一类颜色"时优先用颜色名。

## 场景决策树

1. **抠纯色背景**（如白底产品图）→ 加 `--background-only`，保护物体内部同色区域：
   ```bash
   python3 scripts/color_remover.py product.jpg --color "白色" --background-only
   ```
2. **背景有渐变或色差** → 提高容差，如 `-t 40`
3. **抠完后边缘有毛边/锯齿** → 加 `-f 1` 或 `-f 2` 羽化边缘
4. **抠掉大片颜色后残留零散小点** → 加 `--min-region 50`（按图片尺寸调整）
5. **只保留某颜色、其余透明** → 加 `--invert`
6. **抠完发现误伤了不该抠的区域** → 降低容差 `-t 10`，或改用精确 hex 值

## 注意事项

- 输出必须是 PNG/WebP 才能保留透明通道，脚本强制输出 PNG。
- JPEG 输入正常支持（自动转 RGBA）。
- 脚本运行后会在 stdout 报告抠掉的像素占比；若占比 > 95% 或 < 0.5%，提示用户检查容差设置。
- 脚本依赖 Pillow + numpy（scipy 可选，用于 `--background-only` 和 `--min-region`）。
- 若用户图片需要先用其他方式处理（裁剪、缩放），先处理再抠色。

## 示例

```bash
# 扣掉红色（整个红色系：大红/深红/粉红按规则匹配）
python3 scripts/color_remover.py banner.png --color "红色"

# 去掉白色背景，只要和边缘连通的背景区域
python3 scripts/color_remover.py product.jpg --color "白色" --background-only -f 1

# 扣掉浅蓝色，容差调大，平滑边缘
python3 scripts/color_remover.py sky.png --color "浅蓝" -t 40 -f 2

# 精确抠 #00A651 附近颜色
python3 scripts/color_remover.py logo.png --color "#00A651" -t 15

# 只保留红色，其余全部透明
python3 scripts/color_remover.py poster.png --color "红" --invert
```
